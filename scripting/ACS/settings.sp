
#define SETTING_MAX_COOP_FAILURES_DEFAULT     20
#define SETTING_MAX_COOP_FAILURES_DEFAULT_STR "20"
#define SETTING_MAX_COOP_FAILURES_CVAR_NAME   "acs_max_coop_failures"
#define SETTING_MAX_COOP_FAILURES_CVAR_DESC   "The amount of times the survivors can fail a Coop map before it switches to the next campaign [0 = INFINITE FAILURES]"
